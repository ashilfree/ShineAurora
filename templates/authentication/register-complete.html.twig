{% extends 'base.html.twig' %}

{% block body %}

    {% include '_banner.html.twig' %}
    <div style="padding-top: 60px">
        <div class="container">
            <div>
                <ul>
                    <li class="text-center" style="color: #595959;font-size: 25px;"><a href="{{ path('home', { locale: 'en' }) }}">Home</a> / Register</li>
                </ul>
            </div>
        </div>
    </div>
    <!-- Begin Hiraola's Error 404 Page Area -->
    <div class="error404-area">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 ml-auto mr-auto text-center">
                    <div class="search-error-wrapper">
                        <h1>Register</h1>
                        <h2>Thank you for purchasing, Your order is complete</h2>
                        <div class="hiraola-btn-ps_center"></div>
                        <a href="{{ path('home', {locale:'en'}) }}" class="hiraola-error_btn">Back To Home Page</a>
                        <a href="{{ path('products') }}" class="hiraola-error_btn">Continue Shopping</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Hiraola's Error 404 Page End Here -->
{% endblock %}