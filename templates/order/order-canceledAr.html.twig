{% extends 'baseAr.html.twig' %}

{% block body %}
    {% include '_banner.html.twig' %}
    <div style="padding-top: 60px">
        <div class="container">
            <div>
                <ul>
                    <li class="text-center" style="color: #595959;font-size: 25px;"><a href="{{ path('home', { locale: 'ar' }) }}">الصفحة الرئيسية</a> / الطلب ملغى</li>
                </ul>
            </div>
        </div>
    </div>
    <!-- Begin Hiraola's Checkout Area -->
    <!-- Begin Hiraola's Error 404 Page Area -->
    <div class="error404-area">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 ml-auto mr-auto text-center">
                    <div class="search-error-wrapper">
                        <h1>الطلب ملغى</h1>
                        <h2>عذرا ، تم إلغاء طلبك</h2>
                        <div class="hiraola-btn-ps_center"></div>
                        <a href="{{ path('home', {locale:'ar'}) }}" class="hiraola-error_btn">الصفحة الرئيسية</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

